# OpenAI Service — root compose.
#
# Default `docker compose up` brings up:
#   - db        (postgres:16-alpine)
#   - gateway   (build from ./gateway/Dockerfile)
#
# Route selection + payment minting are delegated to the LOC
# (Livepeer Open Clearinghouse) over HTTP — no daemon sidecars or
# unix-socket volumes. Set LOC_BASE_URL + LOC_API_KEY in .env.
#
# Capability workers are external to this repository and compose stack.
#
# The gateway image serves the checked-in SPAs in production. The
# three Lit SPAs still run independently via `node dev-server.js` in
# each web/ subdir for local development.

services:
  db:
    image: postgres:16-alpine
    container_name: openai-service-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-openai_service}
      POSTGRES_USER: ${POSTGRES_USER:-openai_service}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres-dev-password}
    volumes:
      - openai-service-pgdata:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-65432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-openai_service}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: "no"
  gateway:
    build:
      context: .
      dockerfile: gateway/Dockerfile
      args:
        APP_VERSION: "2.0.0"
        VCS_REF: ${GATEWAY_VCS_REF:-local}
    image: openai-service-gateway:${GATEWAY_TAG:-v2.0.0}
    container_name: openai-service-gateway
    depends_on:
      db:
        condition: service_healthy
    # LOC returns a localhost broker URL for the pilot. Host networking is
    # required so 127.0.0.1:8411 resolves to that broker, not this container.
    network_mode: host
    environment:
      # ── server ──
      PORT: "4001"
      HOST: "127.0.0.1"
      BASE_URL: ${BASE_URL:-http://127.0.0.1:4001}
      PUBLIC_SITE_URL: ${PUBLIC_SITE_URL:-http://127.0.0.1:4001}
      PUBLIC_PORTAL_URL: ${PUBLIC_PORTAL_URL:-http://127.0.0.1:4001/portal/}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
      LOG_LEVEL: ${LOG_LEVEL:-info}

      # ── data ──
      DATABASE_URL: postgres://${POSTGRES_USER:-openai_service}:${POSTGRES_PASSWORD:-postgres-dev-password}@127.0.0.1:${POSTGRES_HOST_PORT:-65432}/${POSTGRES_DB:-openai_service}

      # ── SaaS shell auth ──
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
      API_KEY_HASH_PEPPER: ${API_KEY_HASH_PEPPER:-}
      IP_HASH_PEPPER: ${IP_HASH_PEPPER:-}
      METRICS_TOKEN: ${METRICS_TOKEN:-}
      SESSION_TTL_HOURS: ${SESSION_TTL_HOURS:-24}

      # ── email (optional) ──
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_BASE_URL: ${RESEND_BASE_URL:-https://api.resend.com}
      FROM_EMAIL: ${FROM_EMAIL:-OpenAI Service <noreply@example.com>}

      # ── LOC clearinghouse ──
      LOC_BASE_URL: ${LOC_BASE_URL:-http://127.0.0.1:8088}
      LOC_API_KEY: ${LOC_API_KEY:-}
      LOC_TIMEOUT_MS: ${LOC_TIMEOUT_MS:-30000}
      LOC_SETTLE_INTERVAL_MS: ${LOC_SETTLE_INTERVAL_MS:-15000}
      LOC_SETTLE_ALERT_ATTEMPTS: ${LOC_SETTLE_ALERT_ATTEMPTS:-20}
      LOC_OPEN_MAX_ATTEMPTS: ${LOC_OPEN_MAX_ATTEMPTS:-3}
      LOC_MODEL_MAP: ${LOC_MODEL_MAP:-{}}
      REGISTRY_REFRESH_INTERVAL_MS: ${REGISTRY_REFRESH_INTERVAL_MS:-60000}

      # ── /v1/* rate limit ──
      V1_RATE_LIMIT_PER_MINUTE: ${V1_RATE_LIMIT_PER_MINUTE:-60}
      V1_RATE_LIMIT_BURST: ${V1_RATE_LIMIT_BURST:-30}
    restart: "no"

volumes:
  openai-service-pgdata:
