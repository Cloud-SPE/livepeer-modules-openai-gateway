{
  "name": "livepeer-modules-openai",
  "version": "1.4.1",
  "description": "OpenAI Service — drop-in OpenAI-compatible inference gateway backed by Livepeer",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.0.0"
}
