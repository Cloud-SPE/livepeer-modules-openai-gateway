{
  "name": "@livepeer-modules-openai/admin",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "lint": "echo 'no build step — zero-build Lit + esm.sh'",
    "build": "echo 'no build step — zero-build Lit + esm.sh'",
    "test": "echo 'no tests'"
  }
}
